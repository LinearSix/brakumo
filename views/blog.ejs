<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="nav.css" charset="utf-8">
    <link rel="stylesheet" href="shows.css" charset="utf-8">
    <title>Brakumo - Blog</title>
</head>
<body class="bg_home_img">
    <% include partials/div_nav_bar.ejs %>
    <br/><div class="div_shows_container">
            <% for (i=0; i<blogs.length; i++) { %>
                <table class="table_shows_container">
                    <tr>
                        <td class="td_shows_left">
                            <div class="div_shows_venue"><%= blogs[i].blog_title %></div>
                            <% if (blogs[i].show_id) { %>
                                <br/> - in reference to the show on<br/><%= blogs[i].show_date.toDateString() %>
                            <% } %>
                        </td>
                        <td class="td_shows_right">
                            <div class="div_shows_date"><%= blogs[i].blog_date.toDateString() %></div>
                            <div class="div_shows_info"><%= blogs[i].blog_content %></div>
                        </td>
                    </tr>
                </table>
            <% } %>
        </div>
</body>
</html>